# Scripts del workspace

Este directorio es para scripts compartidos entre componentes.

## Dónde están los scripts útiles hoy
- Backend: `backend/uens-backend/tools/scripts/`
 - `dev/`: levantar/bajar BD y correr backend local.
 - `db/`: utilidades de BD (varios aún están como placeholder).
 - `ci/`: scripts para pipeline (placeholders por ahora).
 - `quality/`: lint/format checks (placeholders por ahora).

## Recomendacion
Usa primero el README del backend: `backend/uens-backend/README.md`
